# 미리보기 전용 사본 (배포에는 index.html만 필요)
artifact.html

# Claude Code 로컬 설정
.claude/

# 사진 원본 (용량이 커서 배포에는 불필요)
images/original/

# 파워포인트 결과물 (make-pptx.py 로 다시 만들 수 있습니다)
*.pptx

# WSL 이 만드는 부스러기 파일
*:Zone.Identifier
